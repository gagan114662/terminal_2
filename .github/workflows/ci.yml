name: TermNet CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip packages
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-${{ matrix.python-version }}-
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run pre-commit hooks
      run: |
        pip install pre-commit
        # Run pre-commit and allow formatting changes
        pre-commit run --all-files || true
        # Check if there are still any uncommitted changes after formatting
        if [ -n "$(git status --porcelain)" ]; then
          echo "❌ Pre-commit hooks made changes but they weren't committed:"
          git status --porcelain
          echo "Please run 'pre-commit run --all-files' locally and commit the changes"
          exit 1
        else
          echo "✅ All pre-commit hooks passed without making changes"
        fi

    - name: Lint with ruff (fallback to flake8)
      run: |
        # Try ruff first, fallback to flake8 if not available
        if command -v ruff > /dev/null 2>&1; then
          echo "Using ruff for linting"
          ruff check . --select E,W,F --exclude __pycache__ --line-length 100
        else
          echo "Ruff not available, using flake8"
          pip install flake8
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude __pycache__
        fi

    - name: Test with pytest
      run: |
        pytest -q --tb=short

    - name: Upload coverage as artifact
      if: matrix.python-version == '3.12'  # Only upload once
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: .coverage
        if-no-files-found: ignore
        retention-days: 30
